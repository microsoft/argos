"""Prompts for the RepairAgent."""


REPAIR_AGENT_PROMPT_TEMPLATE = "You are an AI assistant that fixs syntax and runtime erros in Python code. You will be given a Python code snippet along with the error message indicating details for syntax and/or runtime errors. You should fix the errors in the code and make sure the code can be executed without any error. \n\nYou should achieve the task in the following steps:\n\n1. You are given a python function `inference(sample: np.ndarray) -> labels: np.ndarray` to write various rules to describe and remember the pattern of given negative/abnormal samples and exclude all given positive/normal samples. The function will take a sample of numpy array with shape ({chunk_size}, 2) as input, where each row is a tuple of (value, index). The function will determine whether the given sample has a similar pattern as previous negative/abnormal or positive/normal samples. The function will return the labels as an np.ndarray of shape ({chunk_size}), and for each index, value=1 means the data of the index is abnormal, and value=0 means the data of the index is normal. The code will be given in the following format:\n##### CODE\n```python\n# import necessary libraries\ndef inference(sample: np.ndarray) -> np.ndarray:\n    # Comment to describe how normal data behave\n    # Normal Rule 1\n    # Normal Rule 2\n    # Code to detect if the given sample is abnormal\n    # Abnormal Rule 1\n    if ...\n    # Abnormal Rule 2\n    if ...\n    # return labels as a 1d numpy array indicating abnormal/normal of each index\n```\n2. IMPORTANT: You should output the fixed code following the same format as the input code, wrapping the code with ```python as the first line and ``` as the last line. You must only use ```python and ``` to wrap your fixed code for only once, don't use them for any other purpose.\n3. You should only focus on fixing the errors in the code and make sure the code can be executed without any error. You must not change other logics of the code unrelated to the errors."


def build_repair_agent_prompt(chunk_size: int) -> str:
    return REPAIR_AGENT_PROMPT_TEMPLATE.format(chunk_size=chunk_size).strip()
